# ================================================
# AIFarm Backend Environment Variables
# Vultr 서버 배포용
# 이 파일을 .env로 복사하여 사용
# ================================================

# ------------------
# Supabase 설정
# ------------------
# Supabase 대시보드 → Settings → API에서 확인
SUPABASE_URL=https://zlowwxgopeafmjeyrltl.supabase.co
SUPABASE_KEY=서비스_롤_키_입력  # service_role key (절대 노출 금지!)

# ------------------
# 서버 설정
# ------------------
HOST=0.0.0.0
PORT=8000
DEBUG=false

# ------------------
# CORS 설정
# ------------------
# 프론트엔드 도메인들 (쉼표로 구분)
CORS_ORIGINS=https://aifarm.vercel.app,https://aifarm-dashboard.vercel.app,http://localhost:3000

# ------------------
# API Keys
# ------------------
# OpenAI API Key (AI 댓글 생성용)
OPENAI_API_KEY=sk-...

# YouTube Data API Key (선택사항)
YOUTUBE_API_KEY=AIza...

# ------------------
# 디바이스 네트워크 설정
# ------------------
# 미니PC들이 연결된 서브넷
PHONEBOARD_SUBNET=192.168.1.0/24

# ADB 설정
ADB_PATH=/usr/bin/adb
ADB_PORT=5555

# ------------------
# 로깅
# ------------------
LOG_LEVEL=INFO
LOG_FILE=/var/log/aifarm/api.log

