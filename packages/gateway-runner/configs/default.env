# ============================================================
# DoAi Gateway - Default Configuration
# ============================================================
# This is the default configuration template for Gateway Runner.
# Copy this file and modify for your specific workstation.
#
# Usage:
#   - Copy to workstation-1.env, workstation-2.env, etc.
#   - Modify settings for each workstation
#   - Run: start-gateway.bat workstation-1.env
#
# ============================================================

# -----------------------------------------------------------
# Server Settings
# -----------------------------------------------------------
# Gateway HTTP server port
PORT=3100

# Node identifier (unique per workstation)
NODE_ID=workstation-default

# -----------------------------------------------------------
# Laixi Connection (Device Control via WebSocket)
# -----------------------------------------------------------
# Enable Laixi WebSocket connection
# Set to 'true' when LAIXI.EXE is running
LAIXI_ENABLED=true

# Laixi WebSocket URL
# Default port is 22221 for LAIXI.EXE
LAIXI_URL=ws://127.0.0.1:22221/

# Command timeout (milliseconds)
LAIXI_TIMEOUT=10000

# Heartbeat interval (milliseconds)
LAIXI_HEARTBEAT_INTERVAL=5000

# -----------------------------------------------------------
# Cloud Gateway Connection (Vultr)
# -----------------------------------------------------------
# Enable connection to cloud gateway
VULTR_ENABLED=false

# Cloud Gateway WebSocket URL
# - Local test: ws://localhost:8000/ws/node
# - Production: ws://158.247.210.152:8000/ws/node
VULTR_URL=ws://localhost:8000/ws/node

# Node authentication secret (Base64 encoded)
# Generate: echo -n "your_secret" | base64 (Linux/Mac)
# Or: [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("your_secret")) (PowerShell)
NODE_SECRET=

# -----------------------------------------------------------
# ADB Settings
# -----------------------------------------------------------
ADB_HOST=127.0.0.1
ADB_PORT=5037

# -----------------------------------------------------------
# Chrome Automation (Puppeteer + CDP over ADB)
# -----------------------------------------------------------
# Enable Chrome automation for Android devices
CHROME_ENABLED=false

# ADB port forwarding base port
CHROME_BASE_PORT=9300

# Maximum concurrent Chrome connections
CHROME_MAX_CONNECTIONS=50

# Idle connection timeout (milliseconds)
CHROME_IDLE_TIMEOUT=300000

# Maximum concurrent Chrome tasks
CHROME_MAX_CONCURRENT_TASKS=10

# -----------------------------------------------------------
# Mock Mode (Development/Testing)
# -----------------------------------------------------------
# Enable mock devices for testing without real devices
MOCK_DEVICES=false

# Number of mock devices when enabled
MOCK_DEVICE_COUNT=5

# -----------------------------------------------------------
# Logging
# -----------------------------------------------------------
# Log level: debug, info, warn, error
LOG_LEVEL=info

# -----------------------------------------------------------
# Supabase (Optional - Cloud Database)
# -----------------------------------------------------------
SUPABASE_URL=
SUPABASE_ANON_KEY=
