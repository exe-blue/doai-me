# 🚀 로컬 실행 가이드

전체 시스템을 로컬에서 테스트하는 방법입니다.

## 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│  Frontend (apps/web)                                            │
│  http://localhost:3000/admin/command                            │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTP
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  Cloud Gateway (services/cloud-gateway)                         │
│  http://localhost:8000                                          │
│  ▪ /api/command - 명령 전송                                     │
│  ▪ /api/nodes - 노드 목록                                       │
│  ▪ /ws/node - WebSocket 연결                                    │
└────────────────────────────┬────────────────────────────────────┘
                             │ WebSocket
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  Local Gateway (local/gateway)                                  │
│  http://localhost:3100                                          │
│  ▪ ADB 디바이스 제어                                            │
│  ▪ Laixi 연결 (선택)                                            │
└────────────────────────────┬────────────────────────────────────┘
                             │ USB/ADB
                             ▼
                    ┌─────────────────┐
                    │  Android Device │
                    │  (S9, etc.)     │
                    └─────────────────┘
```

---

## Step 1: Cloud Gateway 실행

### 옵션 A: Vultr 서버 사용 (권장)
이미 Vultr에서 실행 중이라면 이 단계를 건너뛰세요.

```bash
# 서버 상태 확인 (replace with your actual server IP)
curl http://${VULTR_HOST}:8000/health
```

### 옵션 B: 로컬에서 실행

```bash
cd services/cloud-gateway

# 가상환경 생성 및 활성화
python -m venv venv
# Windows
.\venv\Scripts\activate
# Linux/Mac
source venv/bin/activate

# 의존성 설치
pip install -r requirements.txt

# 실행
python main.py
```

**결과:**
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

---

## Step 2: Local Gateway 실행

```bash
cd local/gateway

# 환경 변수 설정
cp config.example.env .env

# .env 편집 (중요!)
# 로컬에서 cloud-gateway 실행 시:
#   VULTR_URL=ws://localhost:8000/ws/node
# Vultr 서버 사용 시:
#   VULTR_URL=ws://${VULTR_HOST}:8000/ws/node

# 의존성 설치
npm install

# 실행
npm start
```

**결과:**
```
╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   🌉 DoAi-Gateway v2.0                                    ║
║   Dynamic Device Discovery + WebSocket Multiplexing       ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝
[Gateway] ADB 서버 초기화...
[Gateway] ✅ ADB 서버 준비
[Vultr] ✅ 연결됨 (session=abc123)
```

---

## Step 3: Frontend 실행

```bash
cd apps/web

# 환경 변수 설정
echo "NEXT_PUBLIC_GATEWAY_URL=http://localhost:8000" > .env.local
# 또는 Vultr 사용 시:
# echo "NEXT_PUBLIC_GATEWAY_URL=http://${VULTR_HOST}:8000" > .env.local

# 의존성 설치
npm install

# 개발 서버 실행
npm run dev
```

**결과:**
```
▲ Next.js 14.x.x
- Local:        http://localhost:3000
```

---

## Step 4: 테스트

### 4.1 브라우저에서 확인

1. http://localhost:3000/admin/command 접속
2. 연결된 노드가 표시되는지 확인
3. 명령 유형 선택 후 "⚡ 명령 전송" 클릭

### 4.2 API로 직접 테스트

```bash
# 노드 목록 확인
curl http://localhost:8000/api/nodes

# 명령 전송
curl -X POST http://localhost:8000/api/command \
  -H "Content-Type: application/json" \
  -d '{
    "node_id": "node_local_001",
    "action": "SYNC_STATE",
    "device_id": "all",
    "params": {},
    "timeout": 60
  }'
```

---

## ADB 디바이스 없이 테스트

실제 디바이스 없이 테스트하려면 Gateway를 Mock 모드로 실행:

```bash
# local/gateway/.env
MOCK_DEVICES=true
MOCK_DEVICE_COUNT=5
```

또는 Android Emulator 사용:

```bash
# Android Studio Emulator 실행 후
adb devices
# 결과: emulator-5554 device
```

---

## 문제 해결

### "연결된 노드가 없습니다"

1. Cloud Gateway 실행 확인:
   ```bash
   curl http://localhost:8000/health
   ```

2. Gateway 로그 확인:
   ```bash
   # local/gateway 터미널에서
   [Vultr] ✅ 연결됨
   ```

3. 환경 변수 확인:
   ```bash
   # local/gateway/.env
   VULTR_ENABLED=true
   VULTR_URL=ws://localhost:8000/ws/node
   ```

### "ADB 연결 실패"

```bash
# ADB 서버 재시작
adb kill-server
adb start-server
adb devices
```

### "CORS 에러"

Cloud Gateway의 CORS 설정 확인:
```python
# services/cloud-gateway/main.py
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # 또는 특정 도메인
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

## 명령 유형

| 명령 | 설명 | 파라미터 예시 |
|------|------|---------------|
| `WATCH_VIDEO` | 특정 영상 시청 | `{"video_url": "https://..."}` |
| `RANDOM_WATCH` | 랜덤 영상 시청 | `{}` |
| `LIKE_VIDEO` | 현재 영상 좋아요 | `{}` |
| `RESTART_DEVICE` | 디바이스 재부팅 | `{}` |
| `SYNC_STATE` | 상태 동기화 | `{}` |

---

## 전체 흐름 다이어그램

```
┌──────────────────────────────────────────────────────────────────┐
│                        USER ACTION                               │
│                                                                  │
│   Browser → /admin/command → "⚡ 명령 전송" 클릭                  │
└──────────────────────────────┬───────────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────────┐
│                       1. HTTP POST                               │
│                                                                  │
│   POST /api/command                                              │
│   {                                                              │
│     "node_id": "node_local_001",                                 │
│     "action": "RANDOM_WATCH",                                    │
│     "device_id": "all"                                           │
│   }                                                              │
└──────────────────────────────┬───────────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────────┐
│                    2. Cloud Gateway                              │
│                                                                  │
│   - 노드 연결 확인                                                │
│   - COMMAND 메시지 빌드                                          │
│   - WebSocket으로 노드에 전송                                     │
└──────────────────────────────┬───────────────────────────────────┘
                               │ WSS COMMAND
                               ▼
┌──────────────────────────────────────────────────────────────────┐
│                      3. Local Gateway                            │
│                                                                  │
│   VultrClient._handleCommand()                                   │
│   → CommandExecutor.execute()                                    │
│   → ADB shell 명령 실행                                          │
└──────────────────────────────┬───────────────────────────────────┘
                               │ ADB
                               ▼
┌──────────────────────────────────────────────────────────────────┐
│                     4. Android Device                            │
│                                                                  │
│   - YouTube 앱 실행                                               │
│   - 영상 재생                                                     │
│   - 좋아요 등                                                     │
└──────────────────────────────┬───────────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────────┐
│                      5. RESULT 반환                              │
│                                                                  │
│   Device → Gateway → Cloud Gateway → Frontend                    │
│                                                                  │
│   결과: ✅ 성공 (5/5 devices)                                     │
└──────────────────────────────────────────────────────────────────┘
```

---

## Laixi 설정 (옵션)

Laixi를 사용하면 AutoX.js 스크립트로 더 정교한 디바이스 제어가 가능합니다.

### Laixi 위치

```
C:\Program Files\Laixi\
├── Laixi.exe           ← Laixi 앱 (WebSocket 서버)
├── Scripts\            ← 스크립트 폴더
│   └── doai\           ← 프로젝트 스크립트 (심볼릭 링크)
└── tools\platform-tools ← ADB 포함
```

### 스크립트 심볼릭 링크 설정

관리자 권한 PowerShell에서 실행:

```powershell
# 방법 1: 설정 스크립트 사용
cd local\gateway\scripts
powershell -ExecutionPolicy Bypass -File setup_laixi_link.ps1

# 방법 2: 수동 설정
$source = "D:\exe.blue\aifarm\local\gateway\scripts\laixi"
$target = "C:\Program Files\Laixi\Scripts\doai"
New-Item -ItemType SymbolicLink -Path $target -Target $source
```

### Laixi 실행

1. `C:\Program Files\Laixi\Laixi.exe` 실행
2. 디바이스 연결 확인
3. Gateway가 자동으로 `ws://127.0.0.1:22221/`에 연결

### Gateway에서 Laixi 사용

```bash
# local/gateway/.env
LAIXI_ENABLED=true
LAIXI_URL=ws://127.0.0.1:22221/
```

### Laixi 없이 실행

Laixi 없이도 Gateway는 직접 ADB로 동작합니다:

```bash
# local/gateway/.env
LAIXI_ENABLED=false
```

---

## Quick Start (TL;DR)

```bash
# (옵션) Laixi 앱 실행
"C:\Program Files\Laixi\Laixi.exe"

# 터미널 1: Cloud Gateway
cd services/cloud-gateway && python main.py

# 터미널 2: Local Gateway
cd local/gateway && npm start

# 터미널 3: Frontend
cd apps/web && npm run dev

# 브라우저
http://localhost:3000/admin/command
```

---

## 단위 테스트 스크립트

각 컴포넌트를 독립적으로 테스트하려면:

```bash
# Cloud Gateway만 테스트
scripts\test_1_cloud_gateway.bat

# Local Gateway만 테스트
scripts\test_2_local_gateway.bat

# Frontend만 테스트
scripts\test_3_frontend.bat

# 전체 통합 테스트
scripts\test_all.bat
```

---

## 🔒 Security Notes

**Important**: This guide references server IPs and URLs using placeholders like `${VULTR_HOST}`.

- Replace `${VULTR_HOST}` with your actual Vultr server IP address or hostname
- **Never commit** real server IPs, API keys, or credentials to version control
- Use environment variables or `.env` files (add to `.gitignore`)
- For production deployments, use GitHub Secrets or a secrets manager

If credentials were accidentally committed, see the Security Notes sections in the database setup guides for instructions on rotating credentials and cleaning git history.
